modules:
  - type: files
    files:
      - source: system
        destination: /

  - type: dnf
    install:
      packages:
        - firefox
        - firefox-langpacks
        - open-eid
        - breeze-cursor-theme
        - yaru-theme

  - type: gschema-overrides

  - type: signing